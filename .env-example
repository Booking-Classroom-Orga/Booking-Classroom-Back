# Database Configuration (PostgreSQL)
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=booking-classroom-db


# Authentication Configuration
JWT_SECRET=your_secure_jwt_secret_here
JWT_EXPIRES_IN=1d  # Token expiration (e.g.: 15m, 1h, 7d)


# Email Configuration (Test SMTP)
# Using Ethereal.email (https://ethereal.email) for testing:
# - Create free test account at https://ethereal.email/create
# - Emails are captured in Ethereal's web interface
MAIL_HOST=smtp.ethereal.email
MAIL_PORT=587
MAIL_USER=your_ethereal_username@ethereal.email
MAIL_PASS=your_ethereal_generated_password
MAIL_FROM="Classroom Booking System <noreply@yourdomain.com>"